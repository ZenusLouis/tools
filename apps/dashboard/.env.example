# PostgreSQL connection
DATABASE_URL="postgresql://gcs:gcs@localhost:5432/gcs"

# AES-256-GCM key for encrypting API keys (32 bytes = 64 hex chars)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-64-char-hex-string-here"

# JWT auth for dashboard login
# Generate with: openssl rand -base64 48
AUTH_SECRET="your-auth-secret-at-least-32-chars"
ADMIN_EMAIL="admin@gcs.local"
ADMIN_PASSWORD="change-me-before-deploy"

# Path to GlobalClaudeSkills root (for seed script)
CLAUDE_ROOT="d:\\GlobalClaudeSkills"

# Secret shared between hook (local machine) and dashboard API
# Generate with: openssl rand -base64 32
HOOK_SECRET="your-hook-secret-here"

# Optional machine token for local bridge sync (created in Dashboard Settings)
BRIDGE_TOKEN="gcsb_your-bridge-token"

# Dashboard URL (used by hook to POST logs)
DASHBOARD_URL="https://gcs-dashboard.zenus.dev"

# Public API URL (if using BFF pattern)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional staged Spring Boot runtime. Keep disabled until route-by-route comparison passes.
GCS_CORE_API_ENABLED=false
GCS_CORE_API_URL=http://localhost:4000

# Stitch MCP API key (from Google AI Studio — used for design screen generation)
STITCH_API_KEY="AQ.xxx..."
