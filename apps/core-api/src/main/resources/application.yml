server:
  port: ${PORT:4000}

spring:
  application:
    name: gcs-core-api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

gcs:
  runtime:
    mode: ${GCS_CORE_RUNTIME_MODE:contract-only}
