apiVersion: v1
kind: Secret
metadata:
  name: gcs-secrets
  namespace: gcs
type: Opaque
stringData:
  DATABASE_URL: "postgresql://gcs:gcs@postgres-svc:5432/gcs"
  POSTGRES_PASSWORD: "gcs"
  ENCRYPTION_KEY: "REPLACE_WITH_OUTPUT_OF: openssl rand -hex 32"
