# Provider Runtime Artifacts

This folder stores generated provider artifacts that are not native to `.claude` or `.codex`.

## Layout

- `providers/chatgpt/`: ChatGPT/OpenAI dashboard-run role prompts and command notes.
- `providers/shared/`: shared runtime snippets for providers that do not need a native folder.
